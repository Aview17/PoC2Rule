# 项目开发说明：PoC2Rule Agent

## 一、项目目标

开发一个基于 Python 的 Agent 工程。

项目目标：

输入以下任意一种形式：

1. Python PoC（poc.py）
2. HTTP 请求明文（request.txt）
3. PoC URL（例如 https://xxx.com/poc.py，可自动下载解析）

输出：

- PCAP
- Detection IR（中间检测规则）
- Snort Rule
- 后续支持其它规则格式（Suricata、自定义规则等）
- 规则测试报告
- 中间分析结果

项目要求：

整个项目采用：

> Python Workflow + LLM Inference + Planner Agent + Plugin

而不是一个巨大的 Prompt 或一个巨大的 SKILL。

整个流程由 Python 控制。

LLM 只负责推理和规划。

---

# 二、总体架构

采用五层架构：

```txt
Workflow(Python)
        │
        ▼
 Planner(局部Agent)
        │
        ▼
 Inference(LLM推理)
        │
        ▼
 Plugin(规则格式)
        │
        ▼
 Tool(Python执行)
```

各层职责如下：

## Workflow

负责整个流程控制。

例如：

```txt
       输入
        │
        ▼
      PoC分析
        │
        ▼
      Pcap生成
        │
        ▼
      Pcap解析
        │
        ▼
      漏洞分类
        │
        ▼
  Detection IR生成
        │
        ▼
      规则生成
        │
        ▼
      规则测试
        │
        ▼
      规则优化
        │
        ▼
       输出
```

Workflow 不允许由 LLM 决定流程。

Workflow 使用 Python 编写。

推荐 LangGraph。

---

## Planner

Planner 是局部 Agent。

负责：

当前节点如何完成。

例如：

PCAP生成。

Planner 自己决定：

- AST恢复请求
- Hook requests
- Hook aiohttp
- Docker运行
- tcpdump抓包
- Scapy生成

Planner 可以调用多个 Tool。

Planner 可以循环：

```txt
      Thought
        │
        ▼
       Tool
        │
        ▼
    Observation
        │
        ▼
      Thought
        │
        ▼
      Finish
```

Planner 不负责整个项目。

只负责一个节点。

---

## Inference

Inference 负责：

一次 LLM 推理。

例如：

```txt
PoC分析。
漏洞分类。
Detection IR生成。
规则生成。
```

> Inference 不负责 Tool 调度。
>
> Inference 不循环。

输入：

```txt
Skill
Knowledge
Example
Input
```

输出：

```txt
JSON
```

---

## Plugin

Plugin 用于支持不同规则格式。

例如：

- Snort
- Suricata
- 用户自定义的规则类型

以后增加新的规则格式时：**Workflow 不需要修改。**

Plugin 自己负责：

- Renderer
- Validator
- Tester
- Schema
- Examples

---

## Tool

Tool 全部使用 Python。

负责：

- 下载PoC
- Docker
- Scapy
- PCAP解析
- Snort执行
- 命令执行
- HTTP解析
- ...等等

Tool 不允许包含 Prompt。

---

# 三、目录结构

```txt
poc2rule-agent/
│
├── main.py                     # 程序入口
├── workflow.py                 # Workflow定义
├── config.yaml                 # 配置
├── requirements.txt
│
├── app/
│   ├── state.py                # Workflow共享状态
│   ├── context.py              # 全局上下文
│   └── logger.py               # 日志
│
├── llm/
│   ├── client.py               # 大模型统一封装
│   ├── inference.py            # 单次LLM推理
│   ├── planner.py              # Planner Agent
│   ├── prompt_builder.py       # Prompt拼接
│   └── output_parser.py        # JSON解析
│
├── workflow_nodes/
│   ├── input_node.py
│   ├── poc_analysis_node.py
│   ├── pcap_generate_node.py
│   ├── traffic_analysis_node.py
│   ├── vulnerability_classify_node.py
│   ├── detection_generate_node.py
│   ├── rule_generate_node.py
│   ├── rule_validate_node.py
│   ├── rule_test_node.py
│   ├── rule_optimize_node.py
│   └── output_node.py
│
├── planners/
├── tools/
├── inference/
├── plugins/
├── schemas/
├── workspace/
└── logs/
```

---

# 四、各模块职责

## main.py - 程序入口。

负责：

- 读取输入。
- 创建 AgentState。
- 启动 Workflow。

不包含任何业务逻辑。

---

## workflow.py - 工作流程

负责：

- 定义 LangGraph Workflow。
- 定义所有 Node。
- 定义节点之间的流转关系。

Workflow 不直接调用 LLM。

Workflow 只调用 Node。

---

## AgentState - 所有节点共享的数据

至少包含：

- PoC路径
- PoC分析结果
- PCAP路径
- HTTP流量信息
- 漏洞分类结果
- Detection IR
- 规则列表
- 测试结果
- 最终报告

所有节点都读写同一个 State。

---

## workflow_nodes

每个 Node 完成一个业务阶段。

Node 不负责拼 Prompt。

Node 不直接访问 OpenAI。

Node 统一调用：

- nferenceService

或

- PlannerService。

---

## llm/client.py - 统一封装大模型

支持：

- OpenAI
- DeepSeek
- Qwen
- vLLM

禁止业务代码直接调用模型 SDK。

---

## prompt_builder.py

负责读取：

- SKILL.md
- Knowledge
- Examples
- Input
- 自动拼接 Prompt。

**任何 Node 都不允许自己拼 Prompt。**

---

## inference.py - 统一提供一次推理接口

例如：

run()

>  输入：
>
> skill、knowledge、examples、input、output_schema
>
> 输出：
>
> JSON对象。

---

## planner.py - 提供 Planner Agent。

Planner 可以支持：

- 循环调用 Tool。
- 根据 Observation 修改策略。

Planner 返回：

**最终结果。**

---

# 五、运行流程

假设输入：`https://example.com/poc.py`

整个流程如下：

```txt
main.py
  ↓
workflow.run()
  ↓
InputNode
  ↓
下载 PoC
  ↓
保存到 workspace/poc/
  ↓
PoCAnalysisNode
  ↓
调用 InferenceService
  ↓
PromptBuilder
  ↓
读取：inference/poc_analysis/SKILL.md
  ↓
读取：Knowledge
  ↓
读取：Examples
  ↓
调用 LLM
  ↓
输出：PoCInfo(JSON)
  ↓
PCAPGenerateNode
  ↓
调用 Planner
  ↓
读取：planners/pcap_planner/SKILL.md
  ↓
LLM 决定策略：
    例如：Scapy 或 Docker
  ↓
调用对应 Tool
  ↓
生成：traffic.pcap
  ↓
TrafficAnalysisNode
  ↓
PCAPReader
  ↓
解析：
    HTTP
    TCP
    Payload
  ↓
输出：TrafficInfo
  ↓
VulnerabilityNode
  ↓
Inference
  ↓
LLM 分类
  ↓
输出：VulnerabilityInfo
  ↓
DetectionNode
  ↓
Inference
  ↓
生成：Detection IR
  ↓
RuleGenerateNode
  ↓
PluginManager
  ↓
Snort Plugin
  ↓
读取：schema.md
  ↓
Renderer
  ↓
LLM
  ↓
生成：Snort Rule
  ↓
Validator
  ↓
Snort -T
  ↓
Tester
  ↓
Snort Replay
  ↓
失败？
  ↓
RuleOptimizeNode
  ↓
Planner
  ↓
修改 Detection IR
  ↓
重新 Render
  ↓
重新 Test
  ↓
成功
  ↓
OutputNode
  ↓
输出：
    1. PCAP
    2. Rule
    3. Detection IR
    4. Report
```

## 流程拆解

主流程: 

```
main.py
    │
    ▼
workflow.run()
    │
    ▼
InputNode
    │
    ▼
PoCAnalysisNode
    │
    ▼
PCAPGenerateNode
    │
    ▼
TrafficAnalysisNode
    │
    ▼
VulnerabilityClassifyNode
    │
    ▼
DetectionGenerateNode
    │
    ▼
RuleGenerateNode
    │
    ▼
RuleValidateNode
    │
    ▼
RuleTestNode
    │
 ┌──┴───────┐
 │          │
 ▼          ▼
PASS      FAIL
 │          │
 │      RuleOptimizeNode
 │          │
 └──────────┘
      │
      ▼
OutputNode
```

LLM 调用链

```txt
Workflow Node
      │
      ▼
InferenceService
      │
      ▼
PromptBuilder
      │
      ├── SKILL.md
      ├── knowledge/
      └── examples/
      │
      ▼
LLM Client
      │
      ▼
Output Parser
      │
      ▼
AgentState
```

Planner 调用链: 

```txt
PCAP Planner
      │
      ▼
读取 SKILL.md
      │
      ▼
LLM Thought
      │
      ▼
选择 Tool
      │
      ▼
执行 Tool
      │
      ▼
Observation
      │
 ┌────┴────┐
 │         │
否         是
 │         │
 ▼         ▼
继续     返回PCAP
```

一次完整执行示例

```txt
URL
 │
 ▼
下载 PoC
 │
 ▼
PoC 分析（LLM）
 │
 ▼
Planner 决定生成 PCAP 策略
 │
 ▼
Scapy / Docker / Hook
 │
 ▼
PCAP
 │
 ▼
解析 HTTP 特征
 │
 ▼
漏洞分类（LLM）
 │
 ▼
Detection IR（LLM）
 │
 ▼
Snort Plugin
 │
 ▼
生成 Rule
 │
 ▼
Snort Test
 │
 ├── PASS → 输出
 └── FAIL → Rule Optimizer → 重测
```





---

# 六、设计原则

1. Workflow 永远由 Python 控制。

2. LLM 不负责整个流程。

3. Planner 仅负责局部策略规划。

4. Inference 仅负责一次推理。

5. Tool 仅负责执行。

6. Plugin 负责规则格式扩展。

7. Prompt 必须模块化，不允许出现一个几千行的 SKILL。

8. 所有 Prompt 都通过 PromptBuilder 自动组合。

9. 所有 LLM 调用必须经过统一封装。
   1. 后续新增规则格式、新模型、新抓包方式时，不允许修改核心 Workflow，应通过 Plugin 或 Tool 扩展完成。
